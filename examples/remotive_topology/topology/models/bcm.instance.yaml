schema: remotive-topology-instance:0.13

ecus:
  BCM:
    models:
      bcm:
        type: container
        container:
          build:
            dockerfile: ../Dockerfile
          command: python -m bcm
